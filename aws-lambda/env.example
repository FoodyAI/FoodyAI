# Database Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to version control

DB_HOST=your-database-host.amazonaws.com
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_secure_password_here

# Firebase Admin SDK Configuration
# Path to the Firebase service account JSON file (local development)
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# Alternative: Store credentials as individual environment variables (recommended for AWS Lambda)
# Get these values from your Firebase service account JSON file
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
